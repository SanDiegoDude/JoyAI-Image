# The only extra dependency likely missing from a ComfyUI env:
bitsandbytes

# Everything else is handled by setting JOYAI_PATH to your
# JoyAI-Image repo root. No pip install of the main package needed.
#
# Example (add to your shell profile or ComfyUI launch script):
#   export JOYAI_PATH=/path/to/JoyAI-Image
